"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.open = void 0;
const terminal_1 = require("../../utils/terminal");
function open(options) {
    const remote = (0, terminal_1.terminal)("git remote -v");
    const owner = remote.split(":")[1].split("/")[0];
    const repo = remote.split(":")[1].split("/")[1].split(".git")[0];
    let link = `https://github.com/${owner}/${repo}`;
    if (options.pull_request)
        link += "/pulls";
    if (options.issue)
        link += "/issues";
    if (options.action)
        link += "/actions";
    (0, terminal_1.terminal)(`open ${link}`);
    console.log("OK");
}
exports.open = open;
//# sourceMappingURL=index.js.map