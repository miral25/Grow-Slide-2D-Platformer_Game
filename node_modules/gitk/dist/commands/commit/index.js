"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.commit = void 0;
const terminal_1 = require("../../utils/terminal");
function commit(options) {
    const message = options.message;
    const currentBranchName = (0, terminal_1.terminal)("git rev-parse --abbrev-ref HEAD");
    if ((currentBranchName.match(/\//g) || []).length === 2) {
        (0, terminal_1.terminal)(`git commit -m "[${currentBranchName.split("/")[1]}] ${message}"`);
    }
    else {
        (0, terminal_1.terminal)(`git commit -m "[none] ${message}"`);
    }
    console.log("OK");
}
exports.commit = commit;
//# sourceMappingURL=index.js.map